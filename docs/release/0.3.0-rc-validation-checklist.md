# 0.3.0-rc.1 Validation Checklist

## Functional

- [ ] Start/Stop is idempotent.
- [ ] Frames render in Fixed layout.
- [ ] Frames render in ResponsiveWide layout.
- [ ] Frames render in ResponsiveNarrow layout.
- [ ] Header status suffix is correct while screen sharing is active.
- [ ] Chat remains unaffected while screensharing is active.

## Error Handling

- [ ] Capture start failure is handled without crash.
- [ ] Viewer decode failure is handled without crash.
- [ ] Disconnect cleans up sender, receiver, and viewer state.
- [ ] EndSession cleans up sender, receiver, capture, and viewer state.

## Performance

- [ ] Sender frame rate is capped.
- [ ] `FramesDropped > 0` under pressure.
- [ ] Short stress run shows no unbounded memory growth.
- [ ] Performance test is green.

## Packaging

- [ ] `artifacts/releases/0.3.0-rc.1/` contains `nLink-Portable-win-x64-0.3.0-rc.1.zip`.
- [ ] `artifacts/releases/0.3.0-rc.1/` contains `nLink-Setup-win-x64-0.3.0-rc.1.exe`.
- [ ] `artifacts/releases/0.3.0-rc.1/` contains `SHA256SUMS.txt`.

## Determinism

- [ ] No fixed `Thread.Sleep` or direct synchronization `Task.Delay` remains in screenshare tests.
- [ ] GUI harness has no fixed sleeps for input timing.
